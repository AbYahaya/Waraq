# Waraq env template — tracked in git. Real values go in backend/.env (gitignored).
# See /API_KEYS.md for what each variable is for and where to obtain it.

# LLM providers
GOOGLE_AI_API_KEY=
OPENAI_API_KEY=
GOOGLE_CLOUD_VISION_CREDENTIALS_JSON=

# Hadith / Qurʾān sources (M5)
SUNNAH_COM_API_KEY=
DORAR_NET_API_KEY=
HADEETHENC_API_KEY=
QURANENC_API_KEY=

# Ops (M5)
RESEND_API_KEY=
SENTRY_DSN=
CORS_ORIGINS=

# Local infra
DATABASE_URL=postgresql+asyncpg://waraq:waraq@localhost:5432/waraq
REDIS_URL=redis://localhost:6379/0

# Crypto — generate via: openssl rand -hex 32
JWT_SECRET=
